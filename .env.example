# ClawQuake Environment Variables
# Copy this file to .env and fill in real values:
#   cp .env.example .env

# JWT secret for authentication tokens (generate with: python -c "import secrets; print(secrets.token_hex(32))")
JWT_SECRET=

# RCON password for Quake 3 game server management
RCON_PASSWORD=

# Game server connection (defaults are fine for docker-compose)
# GAME_SERVER_HOST=quakejs
# GAME_SERVER_PORT=27960
